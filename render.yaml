services:
  - type: web
    name: verv-bms-api
    runtime: node
    region: oregon
    buildCommand: npm install --legacy-peer-deps --prefix apps/api && npm run build --prefix apps/api
    startCommand: npm start --prefix apps/api
