{
  "name": "verv-bms",
  "version": "1.0.0",
  "workspaces": ["apps/api", "apps/web"],
  "scripts": {
    "build": "cd apps/api && npm install && npm run build && cd ../web && npm install && npm run build",
    "dev:api": "cd apps/api && npm run dev",
    "dev:web": "cd apps/web && npm run dev"
  }
}
